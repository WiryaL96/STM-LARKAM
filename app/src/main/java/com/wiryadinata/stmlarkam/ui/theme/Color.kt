package com.wiryadinata.stmlarkam.ui.theme

import androidx.compose.ui.graphics.Color

// Brand palette (Lari Kampus).
val LarkamBlue = Color(0xFF1565C0)
val LarkamBlueDark = Color(0xFF0D47A1)
val LarkamBlueLight = Color(0xFF5E92F3)
val LarkamGreen = Color(0xFF2E7D32)
val LarkamAmber = Color(0xFFF9A825)

// Semantic colors for the timer cards.
/** Card color while the 25-minute timer is still running. */
val CardRunning = Color(0xFFE8F0FE)
/** Card color once the 25-minute timer has expired. */
val CardExpiredRed = Color(0xFFD32F2F)
val OnCardExpired = Color(0xFFFFFFFF)
